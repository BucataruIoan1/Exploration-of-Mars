export interface DiscoveredBoxes {
    stormDust: { row: number; col: number }[];
    radiation: { row: number; col: number }[];
    aliens: { row: number; col: number }[];
    cliffs: { row: number; col: number }[];
}